server:
  port: 8090
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/projectdb?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
jwt:
  secret:
    key: testsecretkey
 

